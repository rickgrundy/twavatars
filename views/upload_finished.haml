!!!
%html
  %head
    %title Upload Address Book
    %meta{"http-equiv" => "Content-Type", content: "text/html; charset=UTF-8"}
    %meta{name: "viewport", content: "width=device-width, initial-scale=1, maximum-scale=2"}
    %meta{name: "robots", content: "noindex, nofollow"}    
    
    %link{rel: "stylesheet", type: "text/css", href: "http://fonts.googleapis.com/css?family=Roboto+Condensed"}
    %link{rel: "stylesheet", type: "text/css", href: "1140.css"}
    %link{rel: "stylesheet", type: "text/css", href: "style.css"}
        
  %body
    .container
      .row
        .twelvecol
          %header
            %h1 Upload Finished
      
          %h2="Matched Photos (#{@with_photos.size})"
          %pre=@with_photos.map { |p| p "#{p.name} (#{p.photo.filename})" }.join "\n"
        
          %h2="Unused Photos (#{@unused_photos.size})"
          %pre=@unused_photos.map(&:filename).join "\n"
        
          -if @duplicate_photos
            %h2="Duplicate Photos (#{@duplicate_photos.size})"
            %pre=@duplicate_photos.map(&:filename).join "\n"
          
          %h2="Missing Photos (#{@missing_photos.size})"
          %pre=@missing_photos.map(&:name).join "\n"